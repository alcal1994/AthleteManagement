@model IEnumerable<AthleteManagement.Models.Sport>

@{
    ViewBag.Title = "Sports";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Sport1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contact)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Season)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Sport1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Contact)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Season)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Pk_Sport_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Pk_Sport_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Pk_Sport_Id })
        </td>
    </tr>
}

</table>
